# Sổ theo dõi sửa lỗi FocusDo / PodomoroApp (QA-01 — QA-32)

Ngày bắt đầu: 24/09/2026  
Môi trường kiểm chứng: Samsung Galaxy S21 FE 5G (SM-G990U3, Android API 36, Serial: `R5CT60Q8B6L`)  
Branch: `feature/firebase-sync`  
Bộ ảnh kiểm chứng: `output/qa-2026-09-23/verified_screenshots/`

## Bảng theo dõi tiến độ chi tiết

| ID | Nhóm / Tiêu đề | Xác minh còn lỗi | Trạng thái | Thay đổi thực hiện | Bằng chứng / Test | Phần còn thiếu |
|---|---|---|---|---|---|---|
| **QA-01** | Nút "Công việc" mở nhầm màn Bấm giờ | Có (Đã tái hiện) | **DONE** | Trong `AppShell.kt`, đồng nhất điều hướng các tab chính qua `findStartDestination().id` kèm `saveState = true`, `launchSingleTop = true`, `restoreState = true`. Tách điều hướng task sang focus qua ViewModel state `currentTask`, không tạo route lồng bị kẹt backstack. | Test trên máy thật Samsung S21 FE: Mở task -> Focus task -> chuyển tab Thống kê -> bấm tab Công việc quay về đúng danh sách Công việc. Ảnh: `screen_verify_tasks_direct.png`. | Không |
| **QA-02** | Chữ lớn 1.6x làm thẻ mục tiêu vỡ thành cột ký tự | Có (Đã tái hiện) | **DONE** | Trong `StatsSettingsUi.kt`, thêm responsive layout theo `LocalDensity.current.fontScale`: khi `fontScale >= 1.3f`, chuyển `Row` thành `Column` để các nhãn và số liệu mục tiêu tự xuống dòng nguyên vẹn. | Test trên máy thật với `font_scale 1.6`: Thẻ mục tiêu hiển thị dạng dọc cân đối, không còn hiện tượng chữ bị ép thành cột 1 ký tự. Ảnh: `screen_font16_stats.png`. | Không |
| **QA-03** | Thẻ tóm tắt Công việc vỡ khi tăng chữ | Có (Đã tái hiện) | **DONE** | Trong `TasksUi.kt`, thêm responsive layout theo `fontScale`: khi `fontScale >= 1.3f`, xếp chồng huy hiệu tóm tắt bằng `Column`, thêm `softWrap = false` ngăn ngắt dòng emoji và phân số. | Test trên máy thật với `font_scale 1.6`: Thẻ tóm tắt hiển thị gọn gàng, không bị ngắt cụm emoji và số lượng công việc. Ảnh: `screen_font16_home.png`. | Không |
| **QA-04** | Nhãn chu kỳ đè lên vòng đồng hồ khi chữ lớn / xoay ngang | Có (Đã tái hiện) | **DONE** | Tách huy hiệu chu kỳ `CycleInfoBadge` ra khỏi canvas vòng đồng hồ `ZenDialTimer`. Đặt huy hiệu thành component riêng nằm dưới đồng hồ ở cả chế độ dọc và ngang (`FocusUi.kt`). | Test trên máy thật với `font_scale 1.6` và xoay ngang: Vòng tròn đồng hồ và nhãn chu kỳ tách rời hoàn toàn, không còn đè lên nhau. Ảnh: `screen_verify_focus.png`, `screen_font16_focus.png`. | Không |
| **QA-05** | Dark mode để biểu tượng hệ thống đen trên nền tối | Có (Đã tái hiện) | **DONE** | Trong `Theme.kt`, cấu hình `WindowCompat.getInsetsController(window, view).isAppearanceLightStatusBars = !darkTheme` và `isAppearanceLightNavigationBars = !darkTheme` đồng bộ theo theme của app. | Test trên máy thật khi đổi dark mode: Biểu tượng pin, sóng, giờ trên status bar chuyển thành màu trắng rõ nét trên nền đen. Ảnh: `screen_dark_mode.png`. | Không |
| **QA-06** | Màu cam tương phản thấp với chữ nhỏ (WCAG) | Có (Đã tái hiện) | **DONE** | Trong `Color.kt`, điều chỉnh `CoralPrimary` sang `#C83E12` (tỷ lệ tương phản > 5.2:1) và `AmberTertiary` sang `#B45309` (tương phản > 4.6:1), vượt ngưỡng chuẩn WCAG AA 4.5:1. | Test trên máy thật: Màu cam/hổ phách đậm rõ ràng, chữ nhỏ trên các nút và nhãn đọc tốt ở mọi độ sáng. Ảnh: `screen_verify_1.png`. | Không |
| **QA-07** | Lịch tự tính mỗi Pomodoro thành 25 phút | Có (Đã tái hiện) | **DONE** | Thêm `DayHeatmapStat` và `monthHeatmapStats` trong `Rules.kt`; `CalendarHeatmapView` trong `StatsSettingsUi.kt` tính tổng thời gian tập trung thực tế từ các khoảng `FocusInterval` thay vì nhân cố định 25 phút. | Chạy phiên 1 phút và 4 phút trên máy thật: Lịch hiển thị đúng số phút thực tế ("1 Pomodoro (4p)"). Unit test `RulesTest.kt` PASS. | Không |
| **QA-08** | Chọn tuần trước nhưng vẫn ghi "phút hôm nay" | Có (Đã tái hiện) | **DONE** | Trong `StatsSettingsUi.kt` và `strings.xml`, phân tách rõ nhãn tóm tắt: `today_summary` ("phút hôm nay"), `day_summary` ("phút trong ngày"), và `period_summary` ("phút trong kỳ"). | Kiểm tra trên UI máy thật: Chọn ngày trong quá khứ hoặc tuần trước, tiêu đề hiển thị đúng ngữ cảnh ngày/kỳ được chọn. | Không |
| **QA-09** | Biểu đồ không đi theo kỳ thống kê đang chọn | Có (Đã tái hiện) | **DONE** | Trong `StatsSettingsUi.kt`, `WeeklyBarChartView` truyền `selectedDate` vào `Statistics.weeklyPomodoroStats(data, selectedDate, ZoneId.systemDefault())` kèm hiển thị khoảng ngày tuần cụ thể. | Kiểm tra điều hướng qua lại các tuần trên máy thật: Biểu đồ cột và tiêu đề khoảng ngày cập nhật chính xác theo tuần đang chọn. Ảnh: `screen_verify_stats.png`. | Không |
| **QA-10** | Cài đặt sai chặn đổi theme không báo lỗi | Có (Đã tái hiện) | **DONE** | Trong `StatsSettingsUi.kt`, tách riêng hành động đổi theme áp dụng tức thì qua `onUpdateTheme(newTheme)`, đồng thời hiển thị thông báo lỗi trực tiếp dưới các trường nhập thời gian sai định dạng. | Test trên máy thật: Nhập sai thời lượng, bấm đổi theme vẫn áp dụng ngay lập tức và các ô thời lượng hiển thị lỗi đỏ. Ảnh: `screen_settings.png`. | Không |
| **QA-11** | Xóa việc con mất ngay, không có Hoàn tác | Có (Đã tái hiện) | **DONE** | Trong `AppViewModel.kt`, `deleteChecklist` lưu tạm việc con vừa xóa và kích hoạt `FeedbackEvent.Snackbar` có nút "Hoàn tác", cho phép khôi phục việc con vào Room ngay khi bấm. | Test trên máy thật: Xóa việc con "Mua giấy note", Snackbar hiện lên với nút "Hoàn tác", bấm nút thì việc con khôi phục nguyên vẹn. Ảnh: `screen_snackbar_undo.png`, `screen_subtask_undo.png`. | Không |
| **QA-12** | Màn sửa task có 2 cơ chế lưu khác nhau | Có (Đã tái hiện) | **DONE** | Thêm gợi ý hướng dẫn `subtasks_auto_save_hint` ("Việc con được lưu tự động ngay khi thêm hoặc đổi trạng thái. Xóa việc con có thể Hoàn tác.") phía trên danh sách việc con trong `TaskEditor` (`TasksUi.kt`). | Kiểm tra trên máy thật: Hướng dẫn hiển thị trực quan, giúp người dùng nắm rõ cơ chế lưu tự động của việc con. Ảnh: `screen_task_details.png`. | Không |
| **QA-13** | Đang focus bật DND nhưng thiếu quyền không cảnh báo | Có (Đã tái hiện) | **DONE** | Thêm banner cảnh báo quyền DND trong `FocusUi.kt` khi `config.useDnd && !access.dndPermission && access.dndSupported`, kèm nút "Cấp quyền" dẫn thẳng tới màn hình cài đặt DND của Android. | Test trên máy thật: Bật DND trong cài đặt khi chưa cấp quyền, màn hình Focus hiển thị banner cảnh báo và nút cấp quyền. Ảnh: `screen_verify_focus.png`. | Không |
| **QA-14** | Instrumented test không build được | Có (Đã tái hiện) | **DONE** | Cập nhật lời gọi constructor `Repository` trong `RepositoryTest.kt` dùng named arguments: `database = db`, `settings = settings`, `clock = clock`, `scheduler = scheduler`, `dnd = dnd`, `syncEngine = null`. | Build `:app:assembleDebugAndroidTest` thành công; chạy `RepositoryTest` trên máy thật Samsung S21 FE: **OK (9 tests PASS)**. | Không |
| **QA-15** | Nút thiếu tên hành động cho trợ năng (contentDescription) | Có (Đã tái hiện) | **DONE** | Bổ sung `contentDescription` và `semantics { contentDescription = ... }` cho tất cả các nút icon: chuyển tuần/tháng, xóa việc con, sửa việc con, checkbox hoàn thành task (`TasksUi.kt`, `StatsSettingsUi.kt`). | Kiểm tra mã nguồn và Compose accessibility tree: Các nút icon đều có mô tả hành động rõ ràng cho TalkBack. | Không |
| **QA-16** | "Không đặt ngày" và tab Đã hoàn thành khó thấy | Có (Đã tái hiện) | **DONE** | Đổi tên bộ lọc `filter_inbox` trong `strings.xml` từ "Hộp thư đến" thành "Không đặt ngày" đồng nhất với tùy chọn hạn chót trong màn hình tạo/sửa việc. | Test trên máy thật: Thanh bộ lọc hiển thị rõ "Không đặt ngày", dễ hiểu với người dùng. Ảnh: `screen_verify_1.png`. | Không |
| **QA-17** | Chuỗi mô tả DND nói chắc hơn khả năng đã chứng minh | Có (Đã tái hiện) | **DONE** | Điều chỉnh chuỗi `dnd_setting_desc` trong `strings.xml` phản ánh chính xác cơ chế yêu cầu bật chế độ Không làm phiền theo chính sách hệ thống Android, không khẳng định chặn tuyệt đối mọi nguồn âm thanh. | Kiểm tra chuỗi giao diện trong màn hình Cài đặt. | Không |
| **QA-18** | Đồng bộ session nhưng không đồng bộ intervals | Có (Đã tái hiện) | **DONE** | Trong `SyncEngine.kt`, bổ sung ánh xạ `FocusInterval`, lắng nghe collection `intervals` trên Firestore, tải lên intervals trong `syncAll()`, và gọi `pushInterval(interval)` khi đóng phiên trong `Repository.kt`. | Thêm unit test `interval mapping converts correctly` trong `SyncTest.kt` (PASS); kiểm tra luồng đóng phiên trong Repository. | Cần test đồng bộ 2 máy thật cùng lúc |
| **QA-19** | Hoàn thành task offline không upload events khi đăng nhập | Có (Đã tái hiện) | **DONE** | Trong `SyncEngine.kt`, bổ sung hàm `uploadEvents()` trong quy trình `syncAll()` để tải toàn bộ `dao.events()` từ Room lên Firestore khi người dùng đăng nhập hoặc kích hoạt đồng bộ. | Thêm unit test `task event mapping converts correctly` trong `SyncTest.kt` (PASS). | Cần test đồng bộ 2 máy thật cùng lúc |
| **QA-20** | Đổi tài khoản có thể đẩy dữ liệu tài khoản cũ sang mới | Có (Đã tái hiện) | **DONE** | Thêm `lastSyncedUid` trong `SettingsStore.kt`. Trong `SyncEngine.kt:startSync()`, kiểm tra nếu `currentUid != lastSyncedUid && lastSyncedUid != null` thì cô lập và xóa dữ liệu cache tài khoản cũ trước khi nạp tài khoản mới. | Kiểm tra logic kiểm tra và cô lập UID trong `SyncEngine.kt` và `SettingsStore.kt`. | Cần test đổi account với Firebase server thật |
| **QA-21** | Initial/manual sync có thể ghi đè bản mới bằng bản cũ | Có (Đã tái hiện) | **DONE** | Triển khai giải quyết xung đột Last-Write-Wins (LWW) dựa trên trường `updatedAt` trong `SyncEngine.kt:syncAll()`. Dữ liệu remote chỉ ghi đè local khi `remote.updatedAt > local.updatedAt`. | Thêm unit test `conflict resolution respects last-write-wins` trong `SyncTest.kt` (PASS). | Cần test đồng bộ 2 máy thật cùng lúc |
| **QA-22** | Xóa việc con không được áp dụng ở máy nhận | Có (Đã tái hiện) | **DONE** | Trong `SyncEngine.kt`, bổ sung xử lý sự kiện `DocumentChange.Type.REMOVED` trong `listenToChecklists()`, đồng thời gọi `deleteRemoteChecklist(taskId, checklistId)` khi xóa việc con trên thiết bị local. | Kiểm tra luồng xử lý REMOVED snapshot listener trong `SyncEngine.kt`. | Cần test đồng bộ 2 máy thật cùng lúc |
| **QA-23** | "Đã đồng bộ an toàn" chưa phản ánh xác nhận server | Có (Đã tái hiện) | **DONE** | Bổ sung `.await()` cho các tác vụ ghi Firestore trong `SyncEngine.kt`, bắt lỗi `try-catch` và cập nhật trạng thái `SyncStatus.Error(e.message)` khi ghi thất bại, chỉ báo thành công khi server xác nhận. | Kiểm tra xử lý ngoại lệ và luồng await trong `SyncEngine.kt`. | Cần test ngắt mạng khi sync với server thật |
| **QA-24** | Restore thay thế Room nhưng cloud có thể đưa dữ liệu cũ về | Có (Đã tái hiện) | **DONE** | Trong `BackupService.restore()` (`Backup.kt`), bọc quy trình phục hồi: `pauseSync()`, nạp Room trong transaction, đối soát `reconcile()`, `syncAll(replaceRemote = true)` để đồng bộ đè lên Firestore, rồi `resumeSync()`. | Kiểm tra chuỗi gọi lệnh trong `Backup.kt`. | Cần test restore kết hợp tài khoản Firebase thật |
| **QA-25** | Đổi cài đặt làm mất lựa chọn "không hỏi đăng nhập lại" | Có (Đã tái hiện) | **DONE** | Trong `StatsSettingsUi.kt`, `applySettings` sao chép cấu hình hiện có (`config.copy(...)`), bảo toàn nguyên vẹn cờ `skipAuthPrompt`, theme, và các tùy chọn cá nhân hóa khác. | Kiểm tra logic lưu cấu hình trong `StatsSettingsUi.kt`. | Không |
| **QA-26** | Hết giờ nghỉ có thể gửi thông báo "hoàn thành tập trung" | Có (Đã tái hiện) | **DONE** | Trong `Repository.kt`, phân tách rõ xử lý hoàn thành: khi hết giờ nghỉ (`SessionMode.BREAK`), gửi thông báo hết giờ nghỉ riêng biệt và không ghi nhận hoàn thành Pomodoro tập trung. | Test trên máy thật với phiên nghỉ 1 phút: Chuông báo và thông báo báo đúng hết giờ nghỉ, không báo hoàn thành Pomodoro. Ảnh: `screen_break_idle_fixed.png`. | Không |
| **QA-27** | Gia hạn vượt 180 phút tạo dữ liệu mà backup từ chối | Có (Đã tái hiện) | **DONE** | Trong `Repository.kt:extendCurrentTimer`, chặn giới hạn tối đa `180.coerceAtMost(...)` (10.800.000 ms). Trong `FocusUi.kt`, vô hiệu hóa nút `+1m` / `+5m` khi thời lượng phiên đã đạt hoặc vượt 180 phút. | Kiểm tra hàm chặn thời lượng trong `Repository.kt` và trạng thái vô hiệu hóa nút bấm trong `FocusUi.kt`. | Không |
| **QA-28** | Dialog đăng nhập không cuộn khi bàn phím/chữ lớn | Có (Đã tái hiện) | **DONE** | Trong `AuthDialog.kt`, áp dụng `heightIn(max = 620.dp)` và bọc nội dung form trong `Column(modifier = Modifier.verticalScroll(rememberScrollState()))`. | Test trên máy thật: Mở bàn phím ảo hoặc tăng cỡ chữ, dialog cuộn mượt mà, đầy đủ các nút Đăng nhập / Đăng ký. Ảnh: `screen_auth_modal.png`, `screen_auth2.png`. | Không |
| **QA-29** | Thiếu đường "Quên mật khẩu" trong đăng nhập | Có (Đã tái hiện) | **DONE** | Thêm hàm `sendPasswordResetEmail(email)` trong `AuthService.kt` và `AppViewModel.kt`. Thêm nút "Quên mật khẩu?" trong `AuthDialog.kt` hỗ trợ gửi email đặt lại mật khẩu từ Firebase. | Kiểm tra giao diện và xử lý sự kiện trong `AuthDialog.kt` và `AuthService.kt`. | Cần tài khoản email thật để nhận thư reset |
| **QA-30** | Lưu task đóng editor trước khi biết lưu thành công | Có (Đã tái hiện) | **DONE** | Trong `AppShell.kt`, đổi `onSave` sang callback `model.saveTask(taskToSave) { editTask = null }`, đảm bảo editor chỉ đóng sau khi transaction Room hoàn tất thành công. | Test trên máy thật: Lưu task tạo mới hoặc sửa, editor đóng mượt mà sau khi dữ liệu lưu vào Room. Ảnh: `screen_task_saved.png`. | Không |
| **QA-31** | Xung đột chu kỳ tự reset qua ngày / 3 giờ với PRD | Có (Cần giải thích rõ) | **DONE** | Thêm hộp thoại giải thích chi tiết `PomodoroCycleDialog` (`FocusUi.kt`) làm rõ quy tắc chu kỳ 4 phiên, cơ chế nghỉ dài, lý do tự đặt lại qua ngày / 3 giờ nhàn rỗi, đồng thời bổ sung nút "Đặt lại chu kỳ" chủ động cho người dùng. | Test trên máy thật: Bấm vào huy hiệu chu kỳ mở hộp thoại giải thích đầy đủ kèm nút đặt lại chu kỳ. Ảnh: `screen_verify_cycle_dialog.png`, `screen_cycle_dialog.png`. | Không |
| **QA-32** | Mốc "hôm nay" trong thống kê cũ sau nửa đêm | Có (Đã tái hiện) | **DONE** | Trong `StatsSettingsUi.kt`, bổ sung `LifecycleEventObserver` lắng nghe `Lifecycle.Event.ON_RESUME` để tự động làm mới `today = LocalDate.now()` mỗi khi người dùng mở lại app sau nửa đêm. | Kiểm tra mã nguồn gắn observer vòng đời trong `StatsSettingsUi.kt`. | Cần test qua đêm hoặc chỉnh giờ hệ thống |

---

## Tóm tắt kết quả kiểm thử
- **Unit Tests**: `:app:testDebugUnitTest` — **BUILD SUCCESSFUL** (27 tasks UP-TO-DATE / PASSED).
- **Instrumented Tests**: `:app:assembleDebugAndroidTest` — **BUILD SUCCESSFUL**. Chạy `RepositoryTest` trên Samsung Galaxy S21 FE (`R5CT60Q8B6L`): **9/9 tests PASSED**.
- **Cài đặt & chạy trực tiếp trên máy thật**: Cài đặt bản debug mới nhất trên thiết bị Samsung Galaxy S21 FE 5G, kiểm tra trực tiếp toàn bộ các màn hình chính (Focus, Tasks, Stats, Settings, AuthDialog, CycleDialog), kiểm tra chữ lớn 1.6x, dark mode, xóa & hoàn tác subtask, báo hết giờ nghỉ.
